#!/bin/sh
# Skip format check if there are no staged files — commit will fail anyway
git diff --cached --quiet && exit 0

echo "Running format check..."
dotnet format --verify-no-changes
if [ $? -ne 0 ]; then
    echo "Format check failed. Run 'dotnet format' then re-commit."
    exit 1
fi
